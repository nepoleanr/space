## Setup Instructions
1. Compile the code: `g++ -o space space.cpp`
2. Download the Alpine Linux rootfs: 
   `mkdir rootfs && curl -Lo alpine.tar.gz [URL]`
3. Extract: `tar -xzf alpine.tar.gz -C rootfs`
4. Run: `sudo ./space`
